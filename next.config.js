/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'lh3.googleusercontent.com',  // Google profile images
      'googleusercontent.com',      // Alternative Google image domain
    ],
  },
  // Add other Next.js config options here as needed
};

module.exports = nextConfig; 